export type ICollectionMetadata = {
    current_page: number,
    per_page: number,
    total: number,
}

export type BalanceResource = {
    'initialBalance': number,
    'totalCredits': number,
    'totalDebits': number,
    'finalBalance': number,
};
